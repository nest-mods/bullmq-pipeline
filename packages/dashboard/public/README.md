# Browser Assets

Future browser TypeScript, HTML, and CSS assets belong here.

Asset references must remain relative to `import.meta.url` so Bull Board can
preload, bundle, and serve them for both local and HTTPS-loaded extensions.
